import { Style } from 'src/models/Style';

export const styles = {
    banner: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    } as Style,

    banner_h1: {
        color: 'white',
    } as Style,

    banner_p: {
        color: 'white',
    } as Style,
}