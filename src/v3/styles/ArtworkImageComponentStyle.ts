import { Style } from 'src/models/Style';

export const styles = {
    content_img: {
        display: 'flex',
        marginBottom: '10px',
        maxWidth: '100%',
        cursor: 'pointer',
    } as Style,
}