import {
	CSSProperties,
} from 'react';

export const styles = {
	banner: {
		display: 'flex',
		flexDirection: 'column',
		alignItems: 'center',
	} as CSSProperties,

	banner_h1: {
		color: 'white',
	} as CSSProperties,

	banner_p: {
		color: 'white',
	} as CSSProperties,
};
