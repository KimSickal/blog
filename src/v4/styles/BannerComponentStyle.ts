import {
	CSSProperties,
} from 'react';

export const styles: { [key: string]: CSSProperties} = {
	banner: {
		display: 'flex',
		flexDirection: 'column',
		alignItems: 'center',
	},

	banner_h1: {
		color: 'white',
	},

	banner_p: {
		color: 'white',
	},
};
