import {
	CSSProperties,
} from 'react';

export const styles = {
	container: {
		display: 'flex',
		flexDirection: 'column',
		alignItems: 'center',
	} as CSSProperties,


	container_h1: {
		textAlign: 'center',
	} as CSSProperties,

	container_contents: {
		flexDirection: 'column',
		justifyContent: 'center',
		marginLeft: '10px',
		marginRight: '10px',
	} as CSSProperties,
};
